"""
DEPRECATED MODULE
This module was replaced by `app.models.iot_api_models`.
It's intentionally disabled to prevent accidental imports. Use `iot_api_models` instead.
"""

raise ImportError(
    "Legacy IoT API models have been consolidated into app.routers.iot. Import from that unified module instead."
)
